name: Hello world
on: [push]
jobs:
  hello_world_job:
    if: ${{ startsWith(github.head_ref, 'test-') }}
    runs-on: ubuntu-latest
    name: test
    steps:
      - name: test
        run: echo ${{ github.head_ref }}
